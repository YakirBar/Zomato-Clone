pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git branch: 'master', url: 'https://github.com/YakirBar/Zomato-Clone.git'
            }
        }
        stage('Build') {
            steps {
                // Placeholder build step
                sh 'echo "Building application..."'
            }
        }
        // Add more stages as needed
    }
}
